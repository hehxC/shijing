# Domain Docs

本仓库采用单上下文领域文档结构。

## 探索代码前

如文件存在，先读取：

- 根目录的 `CONTEXT.md`
- 与当前工作相关的 `docs/adr/` 决策记录

文件不存在时直接继续，不需要提示创建。领域术语或架构决策实际形成后，
由 `domain-modeling` 等技能按需创建。

## 文件结构

```text
/
├── CONTEXT.md
└── docs/
    └── adr/
```

输出中的领域概念应使用 `CONTEXT.md` 定义的术语。若方案与现有 ADR 冲突，
必须明确指出冲突，而不是静默覆盖。
